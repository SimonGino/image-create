# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# build artifacts
*.tsbuildinfo

# production
/build

# local data: SQLite DB + generated images (never commit)
/data/
# smoke-test scratch DB + images (isolated from real data)
/.smoke-data

# secrets — API keys live in .env / ~/.image-create/config.json only
.env
.env*.local
/config.json

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/
.idea/
